"""Evaluation harness: two baselines against Cascade, on the same cases.

Not part of the running application and not installed with it. Run from
`backend/`:

    python -m eval.run_eval --api https://<host> --admin-token <token>
"""
